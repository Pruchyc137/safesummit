brand/.fonts/
_backup/

# Credentials — never commit (contains ADMIN_API_KEY + admin passwords)
SafeSummit_Admin_Credentials.xlsx
*.xlsx
*credential*
*Credential*
.env
.env.*

# Admin login ใช้ ADMIN_API_KEY (ตรวจฝั่ง server) แล้ว — ห้าม commit รหัสผ่านลง repo สาธารณะ
admin-accounts.js
